run_task:
  name: Run Task
  description: Run a Synology task by name.
  target:
    entity:
      integration: synology_tasks
      domain: sensor
  fields:
    task_name:
      name: Task Name
      description: The name of the task to run.
      required: true
      example: "Backup Photos"
      selector:
        text:
